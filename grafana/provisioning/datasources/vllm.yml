# grafana/provisioning/datasources/vllm.yml
# Auto-provisions Prometheus as datasource in Grafana

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true